import { SearchPedidosPipe } from './search-pedidos.pipe';

describe('SearchPedidosPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPedidosPipe();
    expect(pipe).toBeTruthy();
  });
});
