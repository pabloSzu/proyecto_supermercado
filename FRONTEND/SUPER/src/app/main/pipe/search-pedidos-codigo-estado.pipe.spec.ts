import { SearchPedidosCodigoEstadoPipe } from './search-pedidos-codigo-estado.pipe';

describe('SearchPedidosCodigoEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPedidosCodigoEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
